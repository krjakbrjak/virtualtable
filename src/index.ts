/**
 * Entry point.
 *
 * @author Nikita Vakula <programmistov.programmist@gmail.com>
 */

import VirtualTable from './VirtualTable';
import type { VirtualTableProps } from './VirtualTable';
import type { Result, DataSource, Change, Style } from './helpers/types';

export { VirtualTable };

export type { VirtualTableProps, Result, DataSource, Change, Style };
