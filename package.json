{
    "name": "virtualtable-monorepo",
    "private": true,
    "scripts": {
        "clean": "rm -fr packages/*/dist && rm -fr packages/*/node_modules && rm -fr node_modules"
    },
    "workspaces": [
        "packages/*"
    ]
}
